$(document).ready(function(){ // permet de lancer le script une fois le document prêt
  // aurait également pu s'écrire $(function()) le (document).ready étant compris par défaut
  //$('p').hide();  // cache tous les paragraphes
  //$('.p2').hide();  // cache toutes les balises de class 'p1'
  //$('#div2').hide();  // cache l'id div2
  //$('p:nth-child(2)').hide();
});
